{
	// Place your snippets for python here. Each snippet is defined under a snippet name and has a prefix, body and 
	// description. The prefix is what is used to trigger the snippet and the body will be expanded and inserted. Possible variables are:
	// $1, $2 for tab stops, $0 for the final cursor position, and ${1:label}, ${2:another} for placeholders. Placeholders with the 
	// same ids are connected.
	// Example:
	// "Print to console": {
	// 	"prefix": "log",
	// 	"body": [
	// 		"console.log('$1');",
	// 		"$2"
	// 	],
	// 	"description": "Log output to console"
	// }


	"Default imports": {
		"prefix": "!",
		"body": [
			"import numpy as np\nimport pandas as pd\nimport matplotlib.pyplot as plt\nplt.ion()\nfig, axs = plt.subplots(1, 1)\n$0"
		],
		"description": "Imports defined in ipython"
	},
  "comment.snippets": {
    "prefix": "###",
    "body": "############## ${1/(.*)/${1:/upcase}/}$0",
    "description": "Comment block for snippets"
  },
  "comment.major": {
    "prefix": "#>",
    "body": "#  ============${1/./=/g}============\n#              ${1/(.*)/${1:/upcase}/}\n#  ============${1/./=/g}============\n${0}",
    "description": "Major comment bock"
  },
  "ax.ax": {
    "prefix": "ax",
    "body": "ax=plt.gca()$0",
    "description": "Shorthand for active axis"
  },
  "import.defaults": {
    "prefix": "imp",
    "body": "import pandas as pd, numpy as np, matplotlib.pyplot as plt$0",
    "description": "Import pandas, numpy and pyplot"
  }

}